# --- constants (safe to reuse across envs if they aren’t secrets; but keep secrets unique) ---
STRAVA_CLIENT_ID=155302
CRON_SECRET_KEY=devkey123
INTERNAL_API_KEY=supersecretinternalkey
DEBUG_KEY=openaidebug
ACCESS_TOKEN_EXP=3600
REFRESH_TOKEN_EXP=604800

# --- real secrets (generate unique values per env) ---
STRAVA_CLIENT_SECRET=34d2329501b901b501e1a2c7593c01c6cbd5d79c

# --- staging runtime ---
AUTH_BYPASS=0
FLASK_ENV=staging
IS_LOCAL=false

# If API runs inside Railway and can hit the internal host over TLS, keep sslmode=require only if it supports it
DATABASE_URL=postgresql+psycopg2://postgres:pgWuvwQULlbaPfHfbsOHhtiNBEyXBSZD@postgres-517f41c4.railway.internal:5432/railway?sslmode=require

# Correct URLs (no query params here; server adds ?authed=true&code=... etc.)
STRAVA_REDIRECT_URI=https://api.smartcoach.dev/auth/strava/callback
FRONTEND_REDIRECT=https://app.smartcoach.dev/post-oauth

# Auth0 (audience is the API identifier; usually same across envs)
AUTH0_DOMAIN=dev-ppz6v1x0u18obr81.us.auth0.com
AUTH0_AUDIENCE=https://api.smartcoach.dev
AUTH0_ALGORITHMS=RS256
AUTH0_ISSUER=https://dev-ppz6v1x0u18obr81.us.auth0.com/

# CORS (single line; comma-separated if you need to allow the Railway preview too)
CORS_ORIGINS=https://app.smartcoach.dev

# Cookies (shared parent of API+APP)...
SESSION_COOKIE_SECURE=True
SESSION_COOKIE_SAMESITE=None
SESSION_COOKIE_DOMAIN=app.smartcoach.dev
PREFERRED_URL_SCHEME=https

RUN_STAGING_CRON=true

MIN_ACTIVITIES_REQUIRED=5
MAX_ACTIVITIES_TO_DOWNLOAD=5

DEBUG_AUTH=1

FRONTEND_URL=https://app.smartcoach.dev
