providers = ["...", "python"]

[start]
cmd = '''gunicorn -c gunicorn.conf.py run:app'''

[[processes]]
name = "web"
cmd = "gunicorn -c gunicorn.conf.py run:app"

[[processes]]
name = "cron_scheduler"
cmd = "python src/scripts/metrics_scheduler.py"
